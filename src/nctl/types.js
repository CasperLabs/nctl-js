/**
 * @fileOverview NCTL domain types.
 */

 // Enumeration of NCTL account types.
export const AccountType = Object.freeze({
    // A faucet account.
    FAUCET: 'FAUCET',

    // A user account.
    USER: 'USER',

    // A node operator account.
    NODE: 'NODE',
});
